FOUNDRY_PROJECT_ENDPOINT=https://my-foundry-resource.services.ai.azure.com/api/projects/agent-lab
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-5-mini
# Optional: route the debate tool through the deployed Function app instead of
# running the orchestrator in-process. Leave empty for in-process (default).
AZURE_AGENT_API_URL=
